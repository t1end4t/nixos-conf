Use this guide when working under `nixos/base/hosts/`. See `INDEX.md` for the navigation map.

## Scope
- Generated hosts-file wiring.

## Constraints
- Treat `main-hosts` as generated; regenerate via `update-main-hosts.sh`.
- Keep manual additions in `extra-hosts` only.
