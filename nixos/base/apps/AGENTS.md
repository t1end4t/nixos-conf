Use this guide when working under `nixos/base/apps/`. See `INDEX.md` for the navigation map.

## Scope
- Shared system apps/services imported by all hosts.

## Constraints
- Keep host-specific app config in the host folder.
- Put only app/service imports in `default.nix`.
