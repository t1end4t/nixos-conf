Use this guide when working under `nixos/workstation/dns/`. See `INDEX.md` for the navigation map.

## Scope
- Workstation dnscrypt config and name lists.

## Constraints
- Keep laptop DNS changes in its own host folder.
- Keep allow/block/cloak lists as plain text.
