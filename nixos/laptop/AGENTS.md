Use this guide when working under `nixos/laptop/`. See `INDEX.md` for the navigation map.

## Scope
- Laptop-specific NixOS system config.

## Constraints
- Keep shared system config in `../base/`.
- Do not edit `hardware-configuration.nix` unless the task is hardware-specific.
