Use this guide when working under `nixos/laptop/dns/`. See `INDEX.md` for the navigation map.

## Scope
- Laptop dnscrypt config and name lists.

## Constraints
- Keep workstation DNS changes in its own host folder.
- Keep allow/block/cloak lists as plain text.
