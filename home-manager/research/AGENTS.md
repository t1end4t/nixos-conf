Use this guide when working under `home-manager/research/`. See `INDEX.md` for the navigation map.

## Scope
- Research and local LLM user tooling.

## Constraints
- Keep research/local-LLM tools here, not in general CLI tools.
- Put only research imports in `default.nix`.
