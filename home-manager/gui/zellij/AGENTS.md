Use this guide when working under `home-manager/gui/zellij/`. See `INDEX.md` for the navigation map.

## Scope
- Zellij Home Manager config.

## Constraints
- Keep Home Manager wiring in `zellij.nix`.
- Keep Zellij-native config in `config.kdl`.
