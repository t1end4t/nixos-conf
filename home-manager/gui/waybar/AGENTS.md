Use this guide when working under `home-manager/gui/waybar/`. See `INDEX.md` for the navigation map.

## Scope
- Waybar Home Manager config and styling.

## Constraints
- Keep module/settings changes in `settings.nix`.
- Keep CSS-only changes in `style.css`.
- Keep Home Manager wiring in `waybar.nix`.
