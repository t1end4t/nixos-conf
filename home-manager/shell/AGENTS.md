Use this guide when working under `home-manager/shell/`. See `INDEX.md` for the navigation map.

## Scope
- User shells and shell-native config.

## Constraints
- Keep shell-specific config in its matching subfolder.
- Put only shell imports in `default.nix`.
