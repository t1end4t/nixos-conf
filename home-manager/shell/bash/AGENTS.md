Use this guide when working under `home-manager/shell/bash/`. See `INDEX.md` for the navigation map.

## Scope
- Bash Home Manager config and rc content.

## Constraints
- Keep Nix wiring in `bash.nix`.
- Keep shell code in `bashrc`.
