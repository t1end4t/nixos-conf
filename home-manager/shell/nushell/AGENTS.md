Use this guide when working under `home-manager/shell/nushell/`. See `INDEX.md` for the navigation map.

## Scope
- Nushell Home Manager config and extra Nu scripts.

## Constraints
- Keep Nix wiring in `nushell.nix`.
- Keep Nu config/env in `.nu` files.
