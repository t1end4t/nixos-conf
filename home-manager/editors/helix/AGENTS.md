Use this guide when working under `home-manager/editors/helix/`. See `INDEX.md` for the navigation map.

## Scope
- Helix editor config.

## Constraints
- Keep Home Manager wiring in `helix.nix`.
- Keep Helix-native settings in TOML files.
