Use this guide when working under `home-manager/editors/`. See `INDEX.md` for the navigation map.

## Scope
- Editor packages and editor-related user config.

## Constraints
- Keep editor-specific settings in the matching file or subfolder.
- Keep shared language tooling in `lsp.nix`.
- Put only editor imports in `default.nix`.
