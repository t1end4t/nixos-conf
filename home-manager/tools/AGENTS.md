Use this guide when working under `home-manager/tools/`. See `INDEX.md` for the navigation map.

## Scope
- General CLI/TUI tool config.

## Constraints
- Keep GUI apps under `../desktop-apps/`.
- Keep research/local-LLM tools under `../research/`.
- Put only tool imports in `default.nix`.
