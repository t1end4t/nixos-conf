Use this guide when working under `home-manager/desktop-apps/`. See `INDEX.md` for the navigation map.

## Scope
- Desktop applications managed by Home Manager.

## Constraints
- Keep app-specific config in the matching file or subfolder.
- Keep Firefox-specific config in `firefox/`.
- Put only shared desktop-app imports in `default.nix`.
