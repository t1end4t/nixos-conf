Use this guide when working under `home-manager/desktop-apps/firefox/`. See `INDEX.md` for the navigation map.

## Scope
- Firefox Home Manager config, prefs, extensions, and CSS.

## Constraints
- Keep Firefox-only config in this folder.
- Keep browser CSS in the existing CSS files.
- Avoid mixing Betterfox prefs with extension wiring.
